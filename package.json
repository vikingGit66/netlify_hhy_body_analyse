{
    "name": "vercel-mysql",
    "version": "1.0.0",
    "description": "Vercel with MySQL integration",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
      "mysql2": "^3.6.0"
    }
  }